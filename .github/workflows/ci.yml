name: CI Gauntlet

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  gauntlet:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Formatting Check
        run: uv run ruff format --check --output-format=concise

      - name: Linting Check
        run: uv run ruff check --output-format=concise

      # Typst fetches @preview packages (mitex + deps) from
      # packages.typst.org during compile tests; cache them so a registry
      # outage can't flake CI. Bump the key when the template's mitex
      # version changes.
      - name: Cache Typst packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/typst
          key: typst-packages-mitex-0.2.4

      - name: Import Smoke (every shipped module must import)
        env:
          PAPERNEWS_NO_SCHED: "1"
          DEEPSEEK_API_KEY: "dummy-key-for-ci"
        run: uv run python -c "import papernews.serve, papernews.core.main, papernews.__main__"

      - name: The Test Gauntlet
        env:
          PREFECT_TEST_MODE: "true"
          PREFECT_SERVER_ALLOW_EPHEMERAL_MODE: "false"
          PREFECT_API_URL: ""
          DEEPSEEK_API_KEY: "dummy-key-for-ci"
        run: >
          uv run pytest tests/ -n auto -q --tb=short --disable-warnings
          --cov=papernews --cov-report=term --cov-fail-under=80

      - name: Type Checking
        run: uv run mypy papernews/

  docker-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build image
        run: docker build -t papernews:ci .

      - name: Boot container and probe endpoints
        run: |
          # Hermetic ingest: the zero-source smoke config plus wiki disabled
          # means a real run touches no network yet exercises the full
          # flow → render → persistence path. NO_SCHED keeps the startup
          # catch-up off so we can assert the pre-ingest 404 first.
          docker run -d --name papernews-ci \
            -e PAPERNEWS_NO_SCHED=1 \
            -e PAPERNEWS_CONFIG=/app/sources.smoke.toml \
            -e PAPERNEWS_DISABLE_PLUGINS=wiki_plugin \
            -e DEEPSEEK_API_KEY=dummy-key-for-ci \
            -v "$PWD/sources.smoke.toml:/app/sources.smoke.toml:ro" \
            -p 8000:8000 papernews:ci

          # Wait for the server to come up.
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8000/healthz > /dev/null 2>&1; then
              break
            fi
            sleep 2
          done
          curl -fsS http://localhost:8000/healthz
          curl -fsS http://localhost:8000/sources

          # No edition built yet: /digest.pdf must 404 cleanly, not crash.
          code=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/digest.pdf)
          test "$code" = "404"

          # GET /ingest returns the friendly 405 hint.
          code=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/ingest)
          test "$code" = "405"

      - name: Run a real ingest in the container and verify a PDF persists
        run: |
          # Trigger the Prefect pipeline via the same endpoint the NAS uses.
          curl -fsS -X POST http://localhost:8000/ingest

          # Poll for the persisted edition (Prefect boots a temp server per run).
          code=000
          for i in $(seq 1 60); do
            code=$(curl -s -o /tmp/digest.pdf -w '%{http_code}' http://localhost:8000/digest.pdf)
            if [ "$code" = "200" ]; then break; fi
            sleep 2
          done
          test "$code" = "200"
          head -c 4 /tmp/digest.pdf | grep -q '%PDF'

          # The build must have reported success, not a swallowed error.
          curl -fsS 'http://localhost:8000/healthz?deep=1' > /tmp/deep.json
          cat /tmp/deep.json
          grep -q '"status": "error"' /tmp/deep.json && exit 1 || true

      - name: Dump container logs on failure
        if: failure()
        run: docker logs papernews-ci || true
